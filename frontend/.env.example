# Data source: "mock" for in-memory mock data, "http" for real backend API
NEXT_PUBLIC_DATA_SOURCE=mock

# Backend API URL (only used when NEXT_PUBLIC_DATA_SOURCE=http)
# NEXT_PUBLIC_API_URL=http://localhost:8000
